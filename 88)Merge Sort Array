class Solution(object):
    def merge(self, nums1, m, nums2, n):
        i=m-1
        j=n-1
        k=m+n-1
        while (j>=0):
            if(i>=0 && nums[i]>nums[j]):
                nums[k]=nums[i]
                i-=1
            else:
                nums[k]=nums[j]
                j-=1
            k-=1
        return nums
          
