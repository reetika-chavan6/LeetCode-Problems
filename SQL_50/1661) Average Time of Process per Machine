SELECT machine_id, 
round(
    (
sum(
case 
    when activity_type ='end' then timestamp ELSE 0 END)
-
sum( case
        when activity_type='start' then timestamp ELSE 0 END)
    )

/count(Distinct process_id),3)    as processing_time
FROM Activity 
GROUP BY machine_id
