# Write your MySQL query statement below
SELECT 
v.customer_id , count(*) as count_no_trans 
FROM Visits as V
LEFT JOIN
Transactions as T
on V.visit_id =T.visit_id
where T.transaction_id is NULL #filters rows BEFORE GROUP BY
group by Customer_id
