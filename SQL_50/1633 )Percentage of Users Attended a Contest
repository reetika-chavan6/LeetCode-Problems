SELECT R.contest_id,
    ROUND(COUNT(u.user_id) * 100 / (SELECT COUNT(*) FROM Users), 2) 
    as percentage
FROM 
users as u
RIGHT JOIN Register as R
on u.user_id =R.user_id
GROUP BY R.contest_id
order by percentage DESC ,contest_id 
