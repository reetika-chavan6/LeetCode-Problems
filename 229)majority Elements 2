# using hash map
class Solution(object):
    def majorityElement(self, nums):
        res=[]
        dic={}
        for i in nums:
            dic[i]=dic.get(i,0)+1
        s=len(nums)//3
        for i in nums:
            if dic[i]>s and i not in res:
                res.append(i)
        return res
