class Solution(object):
    def numRescueBoats(self, people, limit):
        p=sorted(people)
        l=0
        r=len(p)-1
        boat=0
        while l<=r:
            if p[l]+p[r]<=limit:
                l+=1
            r-=1
            boat+=1
        return boat
       
