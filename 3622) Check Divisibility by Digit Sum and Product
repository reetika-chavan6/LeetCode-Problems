class Solution(object):
    def checkDivisibility(self, n):
        s=str(n)
        sums=0
        prod=1
        for i in range(len(s)):
            sums+=int(s[i])
            prod*=int(s[i])
        if n % (sums+prod) == 0 :
            return True
        else:
            return False
