class Solution(object):
    def maxSubArray(self, nums):
        left=nums[0]
        maximum=nums[0]
        for i in range(1,len(nums)):
            if left<0:
                left=nums[i]
            else:
                left+=nums[i]
            maximum=max(maximum,left)
        return maximum
