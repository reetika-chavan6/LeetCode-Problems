class Solution(object):
    def maxDepth(self, s):
        maxCount=0
        count=0
        for i in s:
            if i=="(":
                count+=1
                maxCount=max(count,maxCount)
            elif i==")" :
                count-=1
        return maxCount
