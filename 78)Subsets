class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        result=[[]]
        for num in nums:
            subsets=[curr+[num] for curr in result]
            result+=subsets
        return result
