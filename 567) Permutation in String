class Solution(object):
    def checkInclusion(self, s1, s2):
        k=len(s1)
        for i in range(k,len(s2)+1):
            if sorted(s2[i-k:i]) == sorted(s1) :
                return True
        return False
