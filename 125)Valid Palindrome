class Solution(object):
    def isPalindrome(self, s):
        s=s.lower()
        r=[]
        for i in s:
            if (i.isalnum()):
                r.append(i)
        clean = ''.join(r)
        if clean[0:]==clean[::-1]:
            return True
        return False
        
